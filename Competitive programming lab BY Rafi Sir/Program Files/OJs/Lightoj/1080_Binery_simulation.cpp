#include <bits/stdc++.h>
using namespace std;
int size,in,en,n,N;
char a[100005],i,q;
void update(int in,int e)
{
	
}
int main()
{
	cin>>N;
	while(N--){
		scanf("%s",a);
		scanf("%c",i);
		if(i=='I')
		{
			scanf("%d %d",&in,&en);
			update(in,en);
		}
	}
	
	return 0;
}
